import React from 'react';
import LibraryAdd from '@material-ui/icons/LibraryAdd';
import MaterialIcon from '../MaterialIcon';

function IconLibraryAdd({ ...rest }) {
  return (
    <MaterialIcon {...rest}>
      <LibraryAdd />
    </MaterialIcon>
  );
}

export default IconLibraryAdd;
