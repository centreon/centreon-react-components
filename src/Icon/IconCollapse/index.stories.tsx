import React from 'react';

import IconCollapse from '.';

export default { title: 'Icon/Collapse' };

export const normal = (): JSX.Element => <IconCollapse />;

export const toggled = (): JSX.Element => <IconCollapse toggled />;
